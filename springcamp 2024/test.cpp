#include <iostream>
#include <math.h>
using namespace std;
// for (int i = 1; i < v.size()-1; i++) {
    //     for (int j : {-1, 0, 1}) for (int k : {-1, 0, 1}) {
    //         if (v[i+j].t == v[i+k].t) {
    //             if (i+j != i+k) res = max(res, v[i+j].l + v[i+k].l);
    //             else res = max(res, v[i+j].l);
    //         }
    //     }
    // }
int main()
{
    // cout << pow(2, (int)log2(11));
    cout << (1<<5);
}