#include <iostream>
using namespace std;

int main()
{
    // freopen("tamgiac.inp", "r", stdin);
    // freopen("tamgiac.out", "w", stdout);

    long long a, b, c; cin >> a >> b >> c;
    cout << a + b + c;
}
