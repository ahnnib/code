#include <iostream>

bool mark[20];
void print()

void snp(int pos)
{
    for (int i = 0; i <= 1; i++)
    {
        mark[pos] = i;
        if (pos == n)
    }
}

int main()
{
    #define task "bignum"
    #ifndef ONLINE_JUDGE
    #else
    freopen(task".inp", "r", stdin);
    freopen(task".out", "w", stdout);
    #endif

    std::string s; int k;
    std::cin >> s >> k;

    int num = 0;
    for (char i : s)
        if ('0' <= i && i <= '9')
            num = num * 10 + i - 48;

    f(0);
}